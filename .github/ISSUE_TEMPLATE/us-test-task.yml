name: "🧪 TEST Task"
description: Final task for testing a User Story in dev environment before deployment in qualif.
title: "[TEST] Implémenter les tests E2E et vérifier que l'US #<number> est complètement fonctionnelle sur dev"
labels:
  - Backend
  - Frontend
  - TEST
type: "task"
projects: ["AVENIRS-Project/16"]
body:
  - type: markdown
    attributes:
      value: |
        ## 🧪 TEST Task – Cofolio

        Final validation of a User Story in dev environment before deployment in qualification.
  - type: input
    id: story_number
    attributes:
      label: Story
      description: Reference the story number this test is related to
      value: "## Story: #"
      placeholder: "## Story: #"
    validations:
      required: true
  - type: textarea
    id: description
    attributes:
      label: Description
      description: List the validation items to be checked in dev environment
      value: |
        - [ ] ⚠️ Implémenter les tests E2E
        - [ ] TODO vérifications sur environnement dev
    validations:
      required: true
  - type: markdown
    attributes:
      value: |
        This task will validate the User Story in dev environment before qualification deployment. It will be automatically added to the 'Cofolio' project.
